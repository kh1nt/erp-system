<UserControl x:Class="erp_system.view.Employees_View"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:view_model="clr-namespace:erp_system.view_model"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <!-- Search + Button -->
                <RowDefinition Height="*"/>
                <!-- Employees List -->
            </Grid.RowDefinitions>

            <!-- ===== TOP BAR ===== -->
            <DockPanel Grid.Row="0" Margin="0,0,0,20">
                <!-- Left: Search Filters -->
                <StackPanel DockPanel.Dock="Left" Orientation="Horizontal" HorizontalAlignment="Left">
                    <!-- Search Box -->
                    <Border Background="White" CornerRadius="6" BorderBrush="#DDD" BorderThickness="1" Padding="10" Width="250">
                        <DockPanel>
                            <TextBlock Text="🔍" VerticalAlignment="Center" Margin="0,0,5,0"/>
                            <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" BorderThickness="0" Background="Transparent"/>
                        </DockPanel>
                    </Border>

                    <!-- Department Filter -->
                    <ComboBox Width="200" Height="40" Padding="5" Background="White" BorderBrush="#DDD" BorderThickness="1"
                              ItemsSource="{Binding Departments}" SelectedItem="{Binding SelectedDepartment}" DisplayMemberPath="DepartmentName">
                    </ComboBox>
                </StackPanel>

                <!-- Right: Add Employee -->
                <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
                    <Button Content="Refresh" Width="100" Height="40" Margin="0,0,10,0"
                            Background="#E5E7EB" Foreground="#111827" FontWeight="Bold" BorderThickness="0" Cursor="Hand"
                            Command="{Binding Refresh_Command}"/>
                    <Button Content=" + Add Employee " Width="140" Height="40"
                            Background="#FACC15" Foreground="Black" FontWeight="Bold" BorderThickness="0" Cursor="Hand"/>
                </StackPanel>
            </DockPanel>

            <!-- ===== EMPLOYEE LIST ===== -->
            <Border Grid.Row="1" Background="White" CornerRadius="8" Padding="20" BorderBrush="#EEE" BorderThickness="1">
                <StackPanel>
                    <TextBlock Text="{Binding FilteredEmployees.Count, StringFormat=All Employees ({0})}" FontWeight="Bold" FontSize="16" Margin="0,0,0,10"/>
                    <DataGrid ItemsSource="{Binding FilteredEmployees}" AutoGenerateColumns="False" HeadersVisibility="Column" CanUserAddRows="False"
                              GridLinesVisibility="None" RowHeaderWidth="0">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="ID" Binding="{Binding EmployeeID}" Width="60"/>
                            <DataGridTextColumn Header="First Name" Binding="{Binding FirstName}" Width="*"/>
                            <DataGridTextColumn Header="Last Name" Binding="{Binding LastName}" Width="*"/>
                            <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="2*"/>
                            <DataGridTextColumn Header="Phone" Binding="{Binding Phone}" Width="*"/>
                            <DataGridTextColumn Header="Position" Binding="{Binding Position}" Width="*"/>
                            <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="*"/>
                            <DataGridTextColumn Header="DepartmentID" Binding="{Binding DepartmentID}" Width="*"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </StackPanel>
            </Border>
        </Grid>
    </ScrollViewer>
</UserControl>