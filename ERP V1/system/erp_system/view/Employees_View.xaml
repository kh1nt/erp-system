<UserControl x:Class="erp_system.view.Employees_View"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:view_model="clr-namespace:erp_system.view_model"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <!-- Search + Button -->
                <RowDefinition Height="*"/>
                <!-- Employees List -->
            </Grid.RowDefinitions>

            <!-- ===== TOP BAR ===== -->
            <DockPanel Grid.Row="0" Margin="0,0,0,20">
                <!-- Left: Search Filters -->
                <StackPanel DockPanel.Dock="Left" Orientation="Horizontal" HorizontalAlignment="Left">
                    <!-- Search Box -->
                    <Border Background="White" CornerRadius="6" BorderBrush="#DDD" BorderThickness="1" Padding="10" Width="250">
                        <DockPanel>
                            <TextBlock Text="🔍" VerticalAlignment="Center" Margin="0,0,5,0"/>
                            <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" BorderThickness="0" Background="Transparent"/>
                        </DockPanel>
                    </Border>

                    <!-- Department Filter -->
                    <ComboBox Width="200" Height="40" Padding="5" Background="White" BorderBrush="#DDD" BorderThickness="1"
                              ItemsSource="{Binding Departments}" SelectedItem="{Binding SelectedDepartment}" DisplayMemberPath="DepartmentName"
                              FontSize="14" HorizontalContentAlignment="Center" VerticalContentAlignment="Center">
                    </ComboBox>
                </StackPanel>

                <!-- Right: Add Employee -->
                <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="Clear" Width="100" Height="40" Margin="0,0,10,0"
                            Background="#E5E7EB" Foreground="#111827" FontWeight="Bold" BorderThickness="0" Cursor="Hand"
                            Command="{Binding Refresh_Command}"/>
                    <Button Content=" + Add Employee " Width="140" Height="40"
                            Background="#FACC15" Foreground="Black" FontWeight="Bold" BorderThickness="0" Cursor="Hand"
                            Command="{Binding AddEmployee_Command}"/>
                </StackPanel>
            </DockPanel>

            <!-- ===== EMPLOYEE LIST ===== -->
            <Border Grid.Row="1" Background="White" CornerRadius="8" Padding="20" BorderBrush="#EEE" BorderThickness="1">
                <StackPanel>
                    <TextBlock Text="{Binding FilteredEmployees.Count, StringFormat=All Employees ({0})}" FontWeight="Bold" FontSize="16" Margin="0,0,0,10"/>
                    <DataGrid ItemsSource="{Binding PagedEmployees}" AutoGenerateColumns="False" HeadersVisibility="Column" CanUserAddRows="False"
                              GridLinesVisibility="None" RowHeaderWidth="0" AlternatingRowBackground="#F8FAFC" 
                              RowHeight="48" FontSize="13" Background="#FFFFFF" BorderThickness="0"
                              CanUserResizeColumns="True" CanUserSortColumns="True"
                              VirtualizingPanel.IsVirtualizing="False" VirtualizingPanel.VirtualizationMode="Standard">
                        <DataGrid.ColumnHeaderStyle>
                            <Style TargetType="DataGridColumnHeader">
                                <Setter Property="Background" Value="#F1F5F9"/>
                                <Setter Property="Foreground" Value="#475569"/>
                                <Setter Property="FontWeight" Value="SemiBold"/>
                                <Setter Property="FontSize" Value="12"/>
                                <Setter Property="Padding" Value="16,12"/>
                                <Setter Property="BorderThickness" Value="0"/>
                                <Setter Property="HorizontalContentAlignment" Value="Center"/>
                                <Setter Property="VerticalContentAlignment" Value="Center"/>
                            </Style>
                        </DataGrid.ColumnHeaderStyle>
                        <DataGrid.RowStyle>
                            <Style TargetType="DataGridRow">
                                <Setter Property="Background" Value="White"/>
                                <Setter Property="BorderThickness" Value="0"/>
                                <Setter Property="Margin" Value="0,0"/>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#F1F5F9"/>
                                    </Trigger>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter Property="Background" Value="#E0F2FE"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </DataGrid.RowStyle>
                        <DataGrid.CellStyle>
                            <Style TargetType="DataGridCell">
                                <Setter Property="Padding" Value="16,12"/>
                                <Setter Property="BorderThickness" Value="0"/>
                                <Setter Property="VerticalAlignment" Value="Center"/>
                                <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                                <Setter Property="Foreground" Value="#1E293B"/>
                                <Style.Triggers>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter Property="Foreground" Value="#0F172A"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </DataGrid.CellStyle>
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="ID" Binding="{Binding EmployeeID}" Width="60" IsReadOnly="True">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="FontWeight" Value="Medium"/>
                                        <Setter Property="Foreground" Value="#64748B"/>
                                        <Setter Property="FontSize" Value="12"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                            <DataGridTextColumn Header="Full Name" Binding="{Binding FullName}" Width="2*" IsReadOnly="True">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="FontWeight" Value="Medium"/>
                                        <Setter Property="Foreground" Value="#1E293B"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                            <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="3*" IsReadOnly="True">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Foreground" Value="#2563EB"/>
                                        <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
                                        <Setter Property="FontWeight" Value="Normal"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                            <DataGridTextColumn Header="Phone" Binding="{Binding Phone}" Width="120" IsReadOnly="True">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Foreground" Value="#64748B"/>
                                        <Setter Property="FontWeight" Value="Normal"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                            <DataGridTextColumn Header="Position" Binding="{Binding Position}" Width="140" IsReadOnly="True">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="FontWeight" Value="Medium"/>
                                        <Setter Property="Foreground" Value="#1E293B"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                            <DataGridTextColumn Header="Basic Salary" Binding="{Binding BasicSalary, StringFormat='{}{0:N2}'}" Width="120" IsReadOnly="True">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="FontWeight" Value="Medium"/>
                                        <Setter Property="Foreground" Value="#059669"/>
                                        <Setter Property="HorizontalAlignment" Value="Center"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                            <DataGridTemplateColumn Header="Status" Width="120" SortMemberPath="Status">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Button Content="{Binding Status}" 
                                                Command="{Binding DataContext.UpdateStatus_Command, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                CommandParameter="{Binding}"
                                                FontWeight="SemiBold"
                                                FontSize="12"
                                                Padding="8,4"
                                                BorderThickness="0"
                                                Cursor="Hand"
                                                ToolTip="Click to open status update dialog">
                                            <Button.Style>
                                                <Style TargetType="Button">
                                                    <!-- Default styling -->
                                                    <Setter Property="Background" Value="#ECFDF5"/>
                                                    <Setter Property="Foreground" Value="#059669"/>
                                                    
                                                    <!-- Active Status - Green -->
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding Status}" Value="Active">
                                                            <Setter Property="Background" Value="#ECFDF5"/>
                                                            <Setter Property="Foreground" Value="#059669"/>
                                                        </DataTrigger>
                                                        
                                                        <!-- Inactive Status - Blue -->
                                                        <DataTrigger Binding="{Binding Status}" Value="Inactive">
                                                            <Setter Property="Background" Value="#EFF6FF"/>
                                                            <Setter Property="Foreground" Value="#2563EB"/>
                                                        </DataTrigger>
                                                        
                                                        <!-- On Leave Status - Yellow -->
                                                        <DataTrigger Binding="{Binding Status}" Value="On Leave">
                                                            <Setter Property="Background" Value="#FFFBEB"/>
                                                            <Setter Property="Foreground" Value="#D97706"/>
                                                        </DataTrigger>
                                                        
                                                        <!-- Terminated Status - Red -->
                                                        <DataTrigger Binding="{Binding Status}" Value="Terminated">
                                                            <Setter Property="Background" Value="#FEF2F2"/>
                                                            <Setter Property="Foreground" Value="#DC2626"/>
                                                        </DataTrigger>
                                                        
                                                        <!-- Hover effects -->
                                                        <Trigger Property="IsMouseOver" Value="True">
                                                            <Setter Property="Background" Value="#D1FAE5"/>
                                                        </Trigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Button.Style>
                                        </Button>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTextColumn Header="Department" Binding="{Binding DepartmentName}" Width="200" IsReadOnly="True">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="HorizontalAlignment" Value="Center"/>
                                        <Setter Property="VerticalAlignment" Value="Center"/>
                                        <Setter Property="FontWeight" Value="SemiBold"/>
                                        <Setter Property="Foreground" Value="#1D4ED8"/>
                                        <Setter Property="FontSize" Value="12"/>
                                        <Setter Property="TextAlignment" Value="Center"/>
                                        <Setter Property="Padding" Value="8,4"/>
                                        <Setter Property="Background" Value="#EFF6FF"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                        </DataGrid.Columns>
                    </DataGrid>

                    <!-- Pagination Controls -->
                    <Border Background="#F8FAFC" CornerRadius="6" Padding="15" Margin="0,20,0,0">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <!-- Page Info -->
                            <TextBlock Grid.Column="0" Text="{Binding PageInfo}" 
                                       FontSize="14" FontWeight="Medium" 
                                       Foreground="#64748B" VerticalAlignment="Center"/>

                            <!-- Pagination Buttons -->
                            <StackPanel Grid.Column="2" Orientation="Horizontal" HorizontalAlignment="Right">
                                <!-- First Page -->
                                <Button Content="⏮" Width="35" Height="35" Margin="2"
                                        Background="White" Foreground="#64748B" 
                                        BorderBrush="#E2E8F0" BorderThickness="1"
                                        FontSize="12" FontWeight="Bold"
                                        Command="{Binding FirstPage_Command}"
                                        ToolTip="First Page">
                                    <Button.Style>
                                        <Style TargetType="Button">
                                            <Style.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="Background" Value="#F1F5F9"/>
                                                </Trigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Button.Style>
                                </Button>

                                <!-- Previous Page -->
                                <Button Content="◀" Width="35" Height="35" Margin="2"
                                        Background="White" Foreground="#64748B" 
                                        BorderBrush="#E2E8F0" BorderThickness="1"
                                        FontSize="12" FontWeight="Bold"
                                        Command="{Binding PreviousPage_Command}"
                                        ToolTip="Previous Page">
                                    <Button.Style>
                                        <Style TargetType="Button">
                                            <Style.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="Background" Value="#F1F5F9"/>
                                                </Trigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Button.Style>
                                </Button>

                                <!-- Current Page Display -->
                                <Border Background="#3B82F6" CornerRadius="4" Padding="12,8" Margin="5,0">
                                    <TextBlock Text="{Binding CurrentPage}" 
                                               FontSize="14" FontWeight="Bold" 
                                               Foreground="White" HorizontalAlignment="Center"/>
                                </Border>

                                <!-- Next Page -->
                                <Button Content="▶" Width="35" Height="35" Margin="2"
                                        Background="White" Foreground="#64748B" 
                                        BorderBrush="#E2E8F0" BorderThickness="1"
                                        FontSize="12" FontWeight="Bold"
                                        Command="{Binding NextPage_Command}"
                                        ToolTip="Next Page">
                                    <Button.Style>
                                        <Style TargetType="Button">
                                            <Style.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="Background" Value="#F1F5F9"/>
                                                </Trigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Button.Style>
                                </Button>

                                <!-- Last Page -->
                                <Button Content="⏭" Width="35" Height="35" Margin="2"
                                        Background="White" Foreground="#64748B" 
                                        BorderBrush="#E2E8F0" BorderThickness="1"
                                        FontSize="12" FontWeight="Bold"
                                        Command="{Binding LastPage_Command}"
                                        ToolTip="Last Page">
                                    <Button.Style>
                                        <Style TargetType="Button">
                                            <Style.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="Background" Value="#F1F5F9"/>
                                                </Trigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Button.Style>
                                </Button>
                            </StackPanel>
                        </Grid>
                    </Border>
                </StackPanel>
            </Border>
        </Grid>
    </ScrollViewer>
</UserControl>