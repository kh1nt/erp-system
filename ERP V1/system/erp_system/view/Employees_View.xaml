<UserControl x:Class="erp_system.view.Employees_View"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">


    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <!-- Search + Button -->
                <RowDefinition Height="*"/>
                <!-- Employees List -->
            </Grid.RowDefinitions>

            <!-- ===== TOP BAR ===== -->
            <DockPanel Grid.Row="0" Margin="0,0,0,20">
                <!-- Left: Search Filters -->
                <StackPanel DockPanel.Dock="Left" Orientation="Horizontal" HorizontalAlignment="Left">
                    <!-- Search Box -->
                    <Border Background="White" CornerRadius="6" BorderBrush="#DDD" BorderThickness="1" Padding="10" Width="250">
                        <DockPanel>
                            <TextBlock Text="🔍" VerticalAlignment="Center" Margin="0,0,5,0"/>
                            <TextBox Text="Search employees..." BorderThickness="0" Background="Transparent"/>
                        </DockPanel>
                    </Border>

                    <!-- Department Filter -->
                    <ComboBox Width="200" Height="40" Padding="5" Background="White" BorderBrush="#DDD" BorderThickness="1">
                        <ComboBoxItem Content="All Departments" IsSelected="True"/>
                        <ComboBoxItem Content="Sales"/>
                        <ComboBoxItem Content="HR"/>
                        <ComboBoxItem Content="Finance"/>
                        <ComboBoxItem Content="IT"/>
                    </ComboBox>
                </StackPanel>

                <!-- Right: Add Employee -->
                <Button DockPanel.Dock="Right" Content=" + Add Employee " Width="140" Height="40" Margin="10,0,0,0"
                        Background="#FACC15" Foreground="Black" FontWeight="Bold" BorderThickness="0" Cursor="Hand"
                        HorizontalAlignment="Right"/>
            </DockPanel>

            <!-- ===== EMPLOYEE LIST ===== -->
            <Border Grid.Row="1" Background="White" CornerRadius="8" Padding="20" BorderBrush="#EEE" BorderThickness="1">
                <StackPanel>
                    <TextBlock Text="All Employees (0)" FontWeight="Bold" FontSize="16" Margin="0,0,0,10"/>
                    <TextBlock Text="No employees found" HorizontalAlignment="Center" VerticalAlignment="Center"
                               Foreground="#666" FontSize="14" Padding="40"/>
                </StackPanel>
            </Border>
        </Grid>
    </ScrollViewer>
</UserControl>
