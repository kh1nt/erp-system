<UserControl x:Class="erp_system.view.Performance_View"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:erp_system.view"
             xmlns:view_model="clr-namespace:erp_system.view_model"
             xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.DataContext>
        <view_model:Performance_View_Model />
    </UserControl.DataContext>
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <!-- Charts Row 1 -->
                <RowDefinition Height="Auto"/>
                <!-- Charts Row 2 -->
                <RowDefinition Height="Auto"/>
                <!-- Performance Table -->
            </Grid.RowDefinitions>

            <!-- ===== PERFORMANCE DISTRIBUTION CHARTS ===== -->
            <Grid Grid.Row="0" Margin="0,0,0,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Score Distribution Chart -->
                <Border Grid.Column="0" Background="White" CornerRadius="8" BorderBrush="#EEE" BorderThickness="1" Padding="20" Margin="5">
                    <StackPanel>
                        <TextBlock Text="Performance Score Distribution" FontWeight="Bold" FontSize="16" Margin="0,0,0,15"/>
                        <lvc:CartesianChart Series="{Binding ScoreDistributionSeries}" Height="250">
                            <lvc:CartesianChart.AxisX>
                                <lvc:Axis Labels="{Binding ScoreDistributionLabels}" FontSize="12"/>
                            </lvc:CartesianChart.AxisX>
                            <lvc:CartesianChart.AxisY>
                                <lvc:Axis LabelFormatter="{Binding CountFormatter}" FontSize="12"/>
                            </lvc:CartesianChart.AxisY>
                        </lvc:CartesianChart>
                    </StackPanel>
                </Border>

                <!-- Department Performance Chart -->
                <Border Grid.Column="1" Background="White" CornerRadius="8" BorderBrush="#EEE" BorderThickness="1" Padding="20" Margin="5">
                    <StackPanel>
                        <TextBlock Text="Department Performance Comparison" FontWeight="Bold" FontSize="16" Margin="0,0,0,15"/>
                        <lvc:CartesianChart Series="{Binding DepartmentPerformanceSeries}" Height="250">
                            <lvc:CartesianChart.AxisX>
                                <lvc:Axis Labels="{Binding DepartmentLabels}" FontSize="12"/>
                            </lvc:CartesianChart.AxisX>
                            <lvc:CartesianChart.AxisY>
                                <lvc:Axis LabelFormatter="{Binding ScoreFormatter}" FontSize="12"/>
                            </lvc:CartesianChart.AxisY>
                        </lvc:CartesianChart>
                    </StackPanel>
                </Border>
            </Grid>

            <!-- ===== SALES PERFORMANCE CHARTS ===== -->
            <Grid Grid.Row="1" Margin="0,0,0,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Sales Amount Trend Chart -->
                <Border Grid.Column="0" Background="White" CornerRadius="8" BorderBrush="#EEE" BorderThickness="1" Padding="20" Margin="5">
                    <StackPanel>
                        <TextBlock Text="Sales Amount Trend" FontWeight="Bold" FontSize="16" Margin="0,0,0,15"/>
                        <lvc:CartesianChart Series="{Binding SalesAmountSeries}" Height="250">
                            <lvc:CartesianChart.AxisX>
                                <lvc:Axis Labels="{Binding SalesTrendLabels}" FontSize="12"/>
                            </lvc:CartesianChart.AxisX>
                            <lvc:CartesianChart.AxisY>
                                <lvc:Axis LabelFormatter="{Binding CurrencyFormatter}" FontSize="12"/>
                            </lvc:CartesianChart.AxisY>
                        </lvc:CartesianChart>
                    </StackPanel>
                </Border>

                <!-- Sales Transaction Count Chart -->
                <Border Grid.Column="1" Background="White" CornerRadius="8" BorderBrush="#EEE" BorderThickness="1" Padding="20" Margin="5">
                    <StackPanel>
                        <TextBlock Text="Sales Transaction Count" FontWeight="Bold" FontSize="16" Margin="0,0,0,15"/>
                        <lvc:CartesianChart Series="{Binding SalesCountSeries}" Height="250">
                            <lvc:CartesianChart.AxisX>
                                <lvc:Axis Labels="{Binding SalesTrendLabels}" FontSize="12"/>
                            </lvc:CartesianChart.AxisX>
                            <lvc:CartesianChart.AxisY>
                                <lvc:Axis LabelFormatter="{Binding CountFormatter}" FontSize="12"/>
                            </lvc:CartesianChart.AxisY>
                        </lvc:CartesianChart>
                    </StackPanel>
                </Border>
            </Grid>

            <!-- ===== PERFORMANCE RECORDS TABLE ===== -->
            <Border Grid.Row="2" Background="White" CornerRadius="8" Padding="20" Margin="0,0,0,20">
                    <StackPanel>
                    <!-- Header with title, filter, and action buttons -->
                    <DockPanel Margin="0,0,0,15">
                        <TextBlock Text="Performance Records" FontSize="22" FontWeight="Bold" DockPanel.Dock="Left"/>
                        
                        <!-- Filter Section -->
                        <StackPanel Orientation="Horizontal" DockPanel.Dock="Left" Margin="20,0,0,0" VerticalAlignment="Center">
                            <TextBlock Text="Filter:" FontWeight="SemiBold" VerticalAlignment="Center" Margin="0,0,10,0"/>
                            <DatePicker SelectedDate="{Binding FilterStartDate}" Width="120" Margin="0,0,10,0"
                                        HorizontalContentAlignment="Left" VerticalContentAlignment="Center"/>
                            <TextBlock Text="to" VerticalAlignment="Center" Margin="0,0,10,0" Foreground="#666"/>
                            <DatePicker SelectedDate="{Binding FilterEndDate}" Width="120" Margin="0,0,10,0"
                                        HorizontalContentAlignment="Left" VerticalContentAlignment="Center"/>
                            <ComboBox ItemsSource="{Binding Departments}" SelectedItem="{Binding SelectedDepartment}" 
                                      Width="150" Height="35" Margin="0,0,10,0"
                                      HorizontalContentAlignment="Left" VerticalContentAlignment="Center"/>
                        </StackPanel>

                        <StackPanel Orientation="Horizontal" DockPanel.Dock="Right" HorizontalAlignment="Right">
                            <Button Content="🔄 Refresh" Command="{Binding Refresh_Command}" Width="100" Height="40" Margin="0,0,10,0"
                                    Background="#E5E7EB" Foreground="#111827" FontWeight="Bold" BorderThickness="0" Cursor="Hand"/>
                            <Button Content="+ Add Review" Command="{Binding AddPerformanceReview_Command}" Width="120" Height="40"
                                    Background="#FACC15" Foreground="White" FontWeight="Bold" BorderThickness="0" Cursor="Hand"/>
                        </StackPanel>
                            </DockPanel>
                    
                    <DataGrid ItemsSource="{Binding PagedPerformanceRecords}" AutoGenerateColumns="False" HeadersVisibility="Column" CanUserAddRows="False"
                              GridLinesVisibility="None" RowHeaderWidth="0" AlternatingRowBackground="#F8FAFC" 
                              RowHeight="48" FontSize="13" Background="#FFFFFF" BorderThickness="0"
                              CanUserResizeColumns="True" CanUserSortColumns="False"
                              VirtualizingPanel.IsVirtualizing="False" VirtualizingPanel.VirtualizationMode="Standard"
                              ScrollViewer.VerticalScrollBarVisibility="Auto" ScrollViewer.HorizontalScrollBarVisibility="Auto"
                              MinHeight="300" MaxHeight="500">
                        <DataGrid.ColumnHeaderStyle>
                            <Style TargetType="DataGridColumnHeader">
                                <Setter Property="Background" Value="#F1F5F9"/>
                                <Setter Property="Foreground" Value="#475569"/>
                                <Setter Property="FontWeight" Value="SemiBold"/>
                                <Setter Property="FontSize" Value="12"/>
                                <Setter Property="Padding" Value="16,12"/>
                                <Setter Property="BorderThickness" Value="0"/>
                                <Setter Property="HorizontalContentAlignment" Value="Center"/>
                                <Setter Property="VerticalContentAlignment" Value="Center"/>
                                <Setter Property="Cursor" Value="Hand"/>
                                <EventSetter Event="Click" Handler="ColumnHeader_Click"/>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#E2E8F0"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </DataGrid.ColumnHeaderStyle>
                        <DataGrid.RowStyle>
                            <Style TargetType="DataGridRow">
                                <Setter Property="Background" Value="White"/>
                                <Setter Property="BorderThickness" Value="0"/>
                                <Setter Property="Margin" Value="0,0"/>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#F1F5F9"/>
                                    </Trigger>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter Property="Background" Value="#E0F2FE"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </DataGrid.RowStyle>
                        <DataGrid.CellStyle>
                            <Style TargetType="DataGridCell">
                                <Setter Property="Padding" Value="16,12"/>
                                <Setter Property="BorderThickness" Value="0"/>
                                <Setter Property="HorizontalAlignment" Value="Center"/>
                                <Setter Property="VerticalAlignment" Value="Center"/>
                                <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                                <Setter Property="Foreground" Value="#1E293B"/>
                                <Style.Triggers>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter Property="Foreground" Value="#0F172A"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </DataGrid.CellStyle>
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="ID" Binding="{Binding RecordID}" Width="60" MinWidth="50" MaxWidth="80"/>
                            <DataGridTextColumn Header="Employee" Binding="{Binding EmployeeName}" Width="*" MinWidth="150"/>
                            <DataGridTextColumn Header="Position" Binding="{Binding Position}" Width="*" MinWidth="120"/>
                            <DataGridTextColumn Header="Department" Binding="{Binding Department}" Width="*" MinWidth="120"/>
                            <DataGridTextColumn Header="Review Date" Binding="{Binding ReviewDate, StringFormat=yyyy-MM-dd}" Width="*" MinWidth="120"/>
                            <DataGridTextColumn Header="Score" Binding="{Binding Score, StringFormat={}{0:F1}}" Width="*" MinWidth="80">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="FontWeight" Value="Bold"/>
                                        <Setter Property="Foreground" Value="#374151"/>
                                        <Style.Triggers>
                                            <!-- High Performance: 4.0 and above -->
                                            <DataTrigger Binding="{Binding Score, Converter={x:Static local:Converters.ScoreToColorConverter}}" Value="High">
                                                <Setter Property="Foreground" Value="#059669"/>
                                            </DataTrigger>
                                            <!-- Good Performance: 3.0 to 3.9 -->
                                            <DataTrigger Binding="{Binding Score, Converter={x:Static local:Converters.ScoreToColorConverter}}" Value="Good">
                                                <Setter Property="Foreground" Value="#10B981"/>
                                            </DataTrigger>
                                            <!-- Average Performance: 2.5 to 2.9 -->
                                            <DataTrigger Binding="{Binding Score, Converter={x:Static local:Converters.ScoreToColorConverter}}" Value="Average">
                                                <Setter Property="Foreground" Value="#F59E0B"/>
                                            </DataTrigger>
                                            <!-- Below Average: 2.0 to 2.4 -->
                                            <DataTrigger Binding="{Binding Score, Converter={x:Static local:Converters.ScoreToColorConverter}}" Value="BelowAverage">
                                                <Setter Property="Foreground" Value="#F97316"/>
                                            </DataTrigger>
                                            <!-- Poor Performance: Below 2.0 -->
                                            <DataTrigger Binding="{Binding Score, Converter={x:Static local:Converters.ScoreToColorConverter}}" Value="Poor">
                                                <Setter Property="Foreground" Value="#EF4444"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                            <DataGridTextColumn Header="Remarks" Binding="{Binding Remarks}" Width="*" MinWidth="200"/>
                        </DataGrid.Columns>
                    </DataGrid>
                    
                    <!-- Pagination Controls -->
                        <Border Background="#F8FAFC" CornerRadius="6" Padding="15" Margin="0,20,0,0">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="{Binding PaginationInfo}" FontSize="14" FontWeight="Medium" Foreground="#64748B" VerticalAlignment="Center"/>
                            <StackPanel Grid.Column="2" Orientation="Horizontal" HorizontalAlignment="Right">
                                <Button Content="⏮" Command="{Binding FirstPage_Command}" Width="32" Height="32" Margin="2" Background="#E5E7EB" BorderThickness="0"/>
                                <Button Content="◀" Command="{Binding PreviousPage_Command}" Width="32" Height="32" Margin="2" Background="#E5E7EB" BorderThickness="0"/>
                                <Border Background="#3B82F6" CornerRadius="4" Padding="12,8" Margin="5,0">
                                    <TextBlock Text="{Binding CurrentPageNumber}" FontSize="14" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center"/>
                                </Border>
                                <Button Content="▶" Command="{Binding NextPage_Command}" Width="32" Height="32" Margin="2" Background="#E5E7EB" BorderThickness="0"/>
                                <Button Content="⏭" Command="{Binding LastPage_Command}" Width="32" Height="32" Margin="2" Background="#E5E7EB" BorderThickness="0"/>
                            </StackPanel>
                        </Grid>
                        </Border>
                    </StackPanel>
                </Border>

        </Grid>
    </ScrollViewer>
</UserControl>

