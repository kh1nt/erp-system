<UserControl x:Class="erp_system.view.Sales_View"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:erp_system.view"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Top Summary Cards -->
        <UniformGrid Grid.Row="0" Columns="3" Margin="0,0,0,15" HorizontalAlignment="Stretch">
            <!-- Total Sales -->
            <Border CornerRadius="10" BorderBrush="#eee" BorderThickness="1" Padding="15" Margin="5" Background="White">
                <StackPanel>
                    <TextBlock Text="Total Sales" FontWeight="Bold" FontSize="14"/>
                    <TextBlock Text="₱0" FontSize="20" FontWeight="Bold" Margin="0,10,0,0"/>
                    <TextBlock Text="📈" FontSize="18" HorizontalAlignment="Right" Foreground="Green"/>
                </StackPanel>
            </Border>

            <!-- This Month -->
            <Border CornerRadius="10" BorderBrush="#eee" BorderThickness="1" Padding="15" Margin="5" Background="White">
                <StackPanel>
                    <TextBlock Text="This Month" FontWeight="Bold" FontSize="14"/>
                    <TextBlock Text="₱0" FontSize="20" FontWeight="Bold" Margin="0,10,0,0"/>
                    <TextBlock Text="🛒" FontSize="18" HorizontalAlignment="Right" Foreground="Goldenrod"/>
                </StackPanel>
            </Border>

            <!-- Total Transactions -->
            <Border CornerRadius="10" BorderBrush="#eee" BorderThickness="1" Padding="15" Margin="5" Background="White">
                <StackPanel>
                    <TextBlock Text="Total Transactions" FontWeight="Bold" FontSize="14"/>
                    <TextBlock Text="0" FontSize="20" FontWeight="Bold" Margin="0,10,0,0"/>
                    <TextBlock Text="🏆" FontSize="18" HorizontalAlignment="Right" Foreground="DodgerBlue"/>
                </StackPanel>
            </Border>
        </UniformGrid>

        <!-- Recent Sales -->
        <Border Grid.Row="1" CornerRadius="10" BorderBrush="#eee" BorderThickness="1" Padding="20" Background="White" Margin="0,0,0,15">
            <StackPanel>
                <TextBlock Text="Recent Sales" FontWeight="Bold" FontSize="16"/>
                <TextBlock Text="No sales recorded yet" Foreground="Gray" HorizontalAlignment="Center" Margin="0,40,0,0"/>
            </StackPanel>
        </Border>

        <!-- Perfume Inventory -->
        <Border Grid.Row="2" CornerRadius="10" BorderBrush="#eee" BorderThickness="1" Padding="20" Background="White">
            <StackPanel>
                <TextBlock Text="Perfume Inventory" FontWeight="Bold" FontSize="16"/>
                <TextBlock Text="No perfumes in inventory" Foreground="Gray" HorizontalAlignment="Center" Margin="0,40,0,0"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
