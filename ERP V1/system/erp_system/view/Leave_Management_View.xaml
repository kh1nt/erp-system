<UserControl x:Class="erp_system.view.Leave_Management_View"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:erp_system.view"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Top Summary Cards -->
        <UniformGrid Grid.Row="0" Columns="3" Margin="0,0,0,15" HorizontalAlignment="Stretch">
            <!-- Pending Approvals -->
            <Border CornerRadius="10" BorderBrush="#eee" BorderThickness="1" Padding="15" Margin="5" Background="White">
                <StackPanel>
                    <TextBlock Text="Pending Approvals" FontWeight="Bold" FontSize="14"/>
                    <TextBlock Text="0" FontSize="20" FontWeight="Bold" Margin="0,10,0,0"/>
                    <TextBlock Text="⏰" FontSize="18" HorizontalAlignment="Right" Foreground="Orange"/>
                </StackPanel>
            </Border>

            <!-- This Month -->
            <Border CornerRadius="10" BorderBrush="#eee" BorderThickness="1" Padding="15" Margin="5" Background="White">
                <StackPanel>
                    <TextBlock Text="This Month" FontWeight="Bold" FontSize="14"/>
                    <TextBlock Text="0" FontSize="20" FontWeight="Bold" Margin="0,10,0,0"/>
                    <TextBlock Text="📅" FontSize="18" HorizontalAlignment="Right" Foreground="DodgerBlue"/>
                </StackPanel>
            </Border>

            <!-- Available Days -->
            <Border CornerRadius="10" BorderBrush="#eee" BorderThickness="1" Padding="15" Margin="5" Background="White">
                <StackPanel>
                    <TextBlock Text="Available Days" FontWeight="Bold" FontSize="14"/>
                    <TextBlock Text="15" FontSize="20" FontWeight="Bold" Margin="0,10,0,0"/>
                    <TextBlock Text="✔️" FontSize="18" HorizontalAlignment="Right" Foreground="Green"/>
                </StackPanel>
            </Border>
        </UniformGrid>

        <!-- Leave Applications -->
        <Border Grid.Row="1" CornerRadius="10" BorderBrush="#eee" BorderThickness="1" Padding="20" Background="White">
            <StackPanel>
                <TextBlock Text="Leave Applications" FontWeight="Bold" FontSize="16"/>
                <TextBlock Text="No leave applications found" Foreground="Gray" HorizontalAlignment="Center" Margin="0,40,0,0"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
