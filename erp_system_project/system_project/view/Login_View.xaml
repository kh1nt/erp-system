<Window x:Class="system_project.view.Login_View"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:system_project.view"
        xmlns:view_model="clr-namespace:system_project.view_model"
        xmlns:custom_controls="clr-namespace:system_project.custom_controls"
        mc:Ignorable="d"
        Title="login_view" Height="550" Width="800"
        WindowStyle="None"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        Background="Transparent"
        AllowsTransparency="True"
        MouseDown="Window_MouseDown">

    <Window.DataContext>
        <view_model:Login_View_Model />
    </Window.DataContext>

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="boolean_to_visibility"/>
    </Window.Resources>
    
    <Window.Visibility>
        <Binding Path="Is_View_Visible" Mode="TwoWay" Converter="{StaticResource boolean_to_visibility}"/>
    </Window.Visibility>
    
    <Border CornerRadius="12">
        <Border.Background>
            <ImageBrush ImageSource="/assets/background.png" 
                            Stretch="Fill"/>
        </Border.Background>
        <Border 
                CornerRadius="10"
                BorderThickness="1"
                Opacity="0.92">
            <Border.BorderBrush>
                <LinearGradientBrush StartPoint="1,1" EndPoint="0,0">
                    <GradientStop Color="{StaticResource PrimaryBluePurple}" Offset="0.5" />
                    <GradientStop Color="{StaticResource PrimaryYellow}" Offset="1" />
                    <GradientStop Color="{StaticResource PrimaryWhite}" Offset="0" />
                </LinearGradientBrush>
            </Border.BorderBrush>

            <Border.Background>
                <LinearGradientBrush StartPoint="0.75,0" EndPoint="1,0.95">
                    <GradientStop Color="{StaticResource PrimaryBluePurple}" Offset="0.10" />
                    <GradientStop Color="{StaticResource PrimaryYellow}" Offset="0.95" />
                </LinearGradientBrush>
            </Border.Background>

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="30" />
                    <RowDefinition/>
                </Grid.RowDefinitions>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition/>
                        <ColumnDefinition Width="40"/>
                        <ColumnDefinition Width="40"/>
                        <ColumnDefinition Width="5"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Text="LOG IN"
                               Foreground="{StaticResource BgLight}"
                               FontSize="15"
                               FontFamily="Segoe UI"
                               Grid.Column="0"
                               VerticalAlignment="Center"
                               Margin="20,0,0,0"/>

                    <Button x:Name="minimize_button"
                            BorderThickness="0"
                            Content="-"
                            Foreground="{StaticResource BgLight}"
                            FontSize="16"
                            FontFamily="Segoe UI"
                            Grid.Column="1"
                            Click="Minimize_Button_Click">

                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Background" Value="Transparent" />
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="{StaticResource BgLight}" />
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>

                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}"
                                        Margin="2"
                                        CornerRadius="5">
                                    <ContentPresenter VerticalAlignment="Center"
                                                      HorizontalAlignment="Center"/>

                                </Border>
                            </ControlTemplate>
                        </Button.Template>

                    </Button>

                    <Button x:Name="close_button"
                            BorderThickness="0"
                            Content="x"
                            Foreground="{StaticResource BgLight}"
                            FontSize="16"
                            FontFamily="Segoe UI"
                            Grid.Column="2"
                            Click="Close_Button_Click">

                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Background" Value="Transparent" />
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="{StaticResource BgLight}" />
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>

                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}"
                                        Margin="2"
                                        CornerRadius="5">
                                    <ContentPresenter VerticalAlignment="Center"
                                                      HorizontalAlignment="Center"/>

                                </Border>
                            </ControlTemplate>
                        </Button.Template>

                    </Button>
                </Grid>

                <StackPanel Width="250"
                            Grid.Row="2"
                            Orientation="Vertical"
                            Margin="0,25,0,0"
                            Background="Transparent">

                    <Image Source="/assets/avielle_logo.png"
                           Width="85"
                           Height="85"/>

                    <TextBlock Text="AVIELLE"
                               Foreground="{StaticResource BgLight}"
                               FontSize="25"
                               FontFamily="Georgia"
                               FontWeight="Bold"
                               HorizontalAlignment="Center"
                               Margin="0,10,0,0"/>


                    <TextBlock Text="Username"
                               Foreground="{StaticResource Color1}"
                               FontSize="15"
                               FontWeight="Medium"
                               FontFamily="Segoe UI"
                               Margin="0,35,0,0"/>

                    <TextBox x:Name="user_text"
                             Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}"
                             FontSize="13"
                             FontWeight="Medium"
                             FontFamily="Segoe UI"
                             Background="Transparent"
                             Foreground="{StaticResource BgLight}"
                             CaretBrush="{StaticResource FontDark}"
                             BorderBrush="{StaticResource BgLight}"
                             BorderThickness="0,0,0,1"
                             Height="28"
                             VerticalContentAlignment="Center"
                             Margin="0,5,0,0"/>

                    <TextBlock Text="Password"
                               Foreground="{StaticResource Color1}"
                               FontSize="15"
                               FontWeight="Medium"
                               FontFamily="Segoe UI"
                               Margin="0,35,0,0"/>

                    <custom_controls:Bindable_Password_Box
                        Password="{Binding Password, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                        Height="28"
                        Margin="0,5,0,0">
                        
                    </custom_controls:Bindable_Password_Box>

                    <TextBlock Text="{Binding Error_Message}"
                               Foreground="{StaticResource ErrorBrush}"
                               FontSize="15"
                               FontWeight="Medium"
                               FontFamily="Segoe UI"
                               Margin="0,10,0,0"/>

                    <Button x:Name="login_button"
                            Command="{Binding Login_Command}"
                            BorderThickness="0"
                            Content="LOG IN"
                            Cursor="Hand"
                            Foreground="#FF2E2E2E"
                            FontSize="16"
                            FontFamily="Calibri"
                            FontWeight="SemiBold"
                            Grid.Column="2"
                            Margin="0,50,0,0">

                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Background" Value="{StaticResource Color1}" />
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="AntiqueWhite" />
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>

                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}"
                                        Height="50"
                                        Width="250"
                                        CornerRadius="10">
                                    <ContentPresenter VerticalAlignment="Center"
                                                      HorizontalAlignment="Center"/>

                                </Border>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>

                    <StackPanel Orientation="Horizontal"
                                HorizontalAlignment="Center"
                                Margin="0,20,0,0"
                                Background="Transparent">
                        
                        <TextBlock Text="Forgot Password?"
                               Foreground="{StaticResource Color1}"
                               FontSize="15"
                               FontFamily="Segoe UI" />

                        <TextBlock Text="Reset"
                               Foreground="{StaticResource BgLight}"
                               FontSize="15"
                               FontFamily="Segoe UI"
                               Cursor="Hand"
                                   Margin="10,0,0,0"/>
                    </StackPanel>

                </StackPanel>
            </Grid>

        </Border>
    </Border>
</Window>